# Policy Model

- Zone-based firewall with nftables backend.
- Candidate/running configs; commit/rollback/commit-confirmed flows.
- Policies reference zones, CIDRs, assets, and ICS protocol primitives (Modbus read/write, function codes, register ranges).
- Identity-based predicates and policy templates (Purdue baseline, maintenance windows) are on the roadmap.

# Policy Model

This document is rendered from `docs/mkdocs/`.

- Zone-based firewall with nftables backend.
- Candidate/running configs; commit/rollback/commit-confirmed flows.
- Policies reference zones, CIDRs, assets, identity (future), ICS protocol primitives (Modbus read/write, function codes, register ranges).
- Templates: Purdue baseline, maintenance window, SIS hardening (future).

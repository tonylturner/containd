# containd UI

Next.js (App Router) front-end for the containd management plane. TailwindCSS and shadcn/ui will be added as features land.

## Getting started

```bash
cd ui
npm install
npm run dev
```

The management API serves the UI build from `ui/public` until a Next.js build artifact is produced.
